@model IEnumerable<CandelariaP.Models.Asiento>

<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Disponible)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCalleNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdZonaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Disponible)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCalleNavigation.IdCalle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdZonaNavigation.IdZona)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAsiento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAsiento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAsiento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

